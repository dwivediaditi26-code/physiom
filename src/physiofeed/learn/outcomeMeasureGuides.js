// outcomeMeasureGuides.js
// Placeholder pending the real content (Aditi has this drafted separately --
// a 207-line file keyed by scale id, each entry `{ how, why }`: `how` is
// short administration guidance, `why` is the clinical rationale). Empty
// for now so OutcomeStudy.jsx's import doesn't break the build; every
// lookup just falls back to `s.adminNote` (already present on some SCALES
// entries) or renders nothing for that InfoBox. Drop the real entries in
// here once provided -- OutcomeStudy.jsx needs no further changes.
export const OUTCOME_GUIDES = {};
